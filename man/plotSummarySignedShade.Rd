% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xQTL_stats.R
\name{plotSummarySignedShade}
\alias{plotSummarySignedShade}
\title{Plot signed -log10(p) with shaded backgrounds}
\usage{
plotSummarySignedShade(
  results,
  simulatedQTL = NULL,
  effective.n.tests = 600,
  p1.name = NULL,
  p2.name = NULL,
  use.LOD = FALSE
)
}
\arguments{
\item{results}{data.frame with columns: physical.position, contrast.beta, p, chrom (and LOD if use.LOD=TRUE)}

\item{simulatedQTL}{data.frame with columns: physical.position, chrom for simulated QTL positions (optional)}

\item{effective.n.tests}{effective number of independent tests genomewide for Bonferroni correction (default 600)}

\item{p1.name}{short name of parent 1 strain (displayed at y-axis max)}

\item{p2.name}{short name of parent 2 strain (displayed at y-axis min)}

\item{use.LOD}{if TRUE, plot signed LOD instead of signed -log10(p) (default FALSE)}
}
\value{
ggplot object
}
\description{
Plots contrast significance as sign(contrast.beta) * -log10(p) per chromosome,
with positive values (mistyrose background) and negative values (lightblue background)
indicating the direction of the allele frequency shift. Bonferroni-corrected
significance thresholds are drawn as horizontal red lines.
}
