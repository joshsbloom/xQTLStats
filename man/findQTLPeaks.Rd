% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xQTL_stats.R
\name{findQTLPeaks}
\alias{findQTLPeaks}
\title{Find QTL peaks and confidence intervals from contrast statistics}
\usage{
findQTLPeaks(
  results,
  LOD.threshold = 4,
  LOD.drop = 2,
  p1.name = "p1",
  p2.name = "p2"
)
}
\arguments{
\item{results}{data.frame output of calcContrastStats with columns:
chrom, physical.position, LOD, contrast.beta}

\item{LOD.threshold}{minimum LOD score for a peak to be called (default 4)}

\item{LOD.drop}{LOD units below the peak to define the confidence interval (default 2)}

\item{p1.name}{short name of parent 1 strain (positive contrast.beta direction)}

\item{p2.name}{short name of parent 2 strain (negative contrast.beta direction)}
}
\value{
GRanges object with metadata columns: peak.pos, peak.LOD, peak.beta,
direction (sign of contrast.beta at peak), high.parent (strain name favored at peak),
low.parent (strain name disfavored at peak)
}
\description{
Identifies QTL peaks that exceed a minimum LOD threshold and defines
confidence intervals using a LOD drop method. Returns a GRanges object
with peak position, LOD score, effect direction, and parent strain
annotations per interval.
}
